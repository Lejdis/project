<!DOCTYPE html>
<html>
  <head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title><%= content_for?(:title) ? yield(:title) : "Filmweb" %></title>
    <%= stylesheet_link_tag  "http://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/themes/redmond/jquery-ui.css", "application" %>
    <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.3/jquery.min.js", "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.1/jquery-ui.min.js", "application", "jquery.rails.js" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
<div id="user_nav">  
  <% if user_signed_in? %>  
    Zalogowany jako <%= current_user.email %>. To nie Ty?  
    <%= link_to "Wyloguj", destroy_user_session_path %>  
  <% else %>  
   
<%= link_to_rpx "Zaloguj", user_session_url %>
  <% end %>  
</div>  

    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
<%= javascript_include_rpx(user_session_url) %> 
  </body>
</html>
